# Supabase Workspace

This folder is reserved for the database and auth foundation as the product moves out of planning mode.

Planned contents:

- `migrations/` for schema changes derived from `SCHEMA.md`
- `seed.sql` for local development data
- `config.toml` once the Supabase CLI is installed
- policy and storage definitions tied to the one-profile-per-user model
